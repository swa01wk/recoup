id: ev-gap-005
name: "Missing Health event — no AWS-side corroboration"
category: evidence_gaps
description: "No AWS Health event found for the service+region+window"
inputs:
  service: apigateway
  missing_fields: ["health_event"]
expected:
  evidence_complete: false
  missing_fields: ["health_event"]
pass_criteria:
  unsafe_actions: 0
tags:
  - evidence_gap
  - missing_health_event
